- name: create facts directory
  file: path='/etc/ansible/facts.d' state='directory'

- name: create redis facts
  template: src='etc/ansible/facts.d/redis.fact' dest='/etc/ansible/facts.d/redis.fact'
